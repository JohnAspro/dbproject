# from flask_wtf import FlaskForm
# from wtforms import StringField, SubmitField
# from wtforms.validators import DataRequired, Email

# ## when passed as a parameter to a template, an object of this class will be rendered as a regular HTML form
# ## with the additional restrictions specified for each field
# class researcher_form(FlaskForm):
#     first_name = StringField(label = "first_name", validators = [DataRequired(message = "Name is a required field.")])

#     last_name = StringField(label = "last_name", validators = [DataRequired(message = "Surname is a required field.")])

#     sex = StringField(label = "Gender", validators = [DataRequired(message = "Surname is a required field.")])

#     submit = SubmitField("Create")
